[General]
job_type = local # local, slurm, sge, pbs, lsf
job_prefix = nextDenovo
task = all
rewrite = yes # yes/no
deltmp = yes 
parallel_jobs = 24 # number of tasks used to run in parallel
input_type = raw # raw, corrected
read_type = ont # clr, ont, hifi
input_fofn = input.fofn
workdir = allreads_rundir

[correct_option]
read_cutoff = 5000
genome_size = 198m # estimated genome size
seed_depth = 60
sort_options = -m 20g -t 15
minimap2_options_raw = -t 8
pa_correction = 12 # number of corrected tasks used to run in parallel, each corrected task requires ~TOTAL_INPUT_BASES/4 bytes of memory usage.
correction_options = -p 15

[assemble_option]
minimap2_options_cns = -t 8 
nextgraph_options = -a 1
